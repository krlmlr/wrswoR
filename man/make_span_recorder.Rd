% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/otel.R
\name{make_span_recorder}
\alias{make_span_recorder}
\title{Record otel spans emitted by wrswoR functions}
\usage{
make_span_recorder()
}
\value{
An environment with two members:
\itemize{
\item \code{spans}: list of recorded spans, each a list with \code{name} and
\code{attributes}
\item \code{start}: function with the same signature as
\code{\link[otel:start_local_active_span]{otel::start_local_active_span()}} that records into \code{spans}
}
}
\description{
Creates an in-memory recorder that captures spans. Use it together with
\code{\link[testthat:local_mocked_bindings]{testthat::local_mocked_bindings()}} to swap \code{start_local_active_span} for
the recorder's capture function in tests:
}
\details{
\if{html}{\out{<div class="sourceCode r">}}\preformatted{recorder <- make_span_recorder()
testthat::local_mocked_bindings(start_local_active_span = recorder$start)
sample_int_rej(6, 3, c(995, rep(1, 5)))
recorder$spans
}\if{html}{\out{</div>}}
}
\examples{
recorder <- make_span_recorder()
span <- recorder$start("demo", attributes = list(x = 1))
span$set_attribute("y", 2)
recorder$spans
}
